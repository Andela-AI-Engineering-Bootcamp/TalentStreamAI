aws_region          = "eu-central-1"
state_bucket_name   = "your-company-talentstreamai-tfstate-UNIQUE"
dynamodb_table_name = "terraform-locks"

# Use true only for disposable sandboxes so terraform destroy can empty the bucket.
force_destroy_state_bucket = false
